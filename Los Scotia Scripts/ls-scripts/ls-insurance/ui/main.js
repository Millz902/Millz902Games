// ls-insurance UI Script
console.log('ls-insurance UI loaded');

window.addEventListener('message', function(event) {
    // Handle NUI messages
    console.log('Message received:', event.data);
});
