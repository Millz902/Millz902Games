// ls-government UI Script
console.log('ls-government UI loaded');

window.addEventListener('message', function(event) {
    // Handle NUI messages
    console.log('Message received:', event.data);
});
